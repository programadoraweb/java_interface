interface  Conta {
    // interface eh uma palavra reservada

    // Nesta classe se listam os Metodos que sao Obrigatorios :
    // So se DECLARA os metodos, nao se implementam aqui.
    // So vamos coclocar a ASSINATURA DO METODO
    void depositar(double valor); 
    void sacar (double valor);
    double getSaldo();  
    
}
